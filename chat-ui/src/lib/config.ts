/**
 * Shared configuration constants for the chat-ui API layer.
 *
 */
import path from "path";

/** Absolute path to the sibling `frontend/` project directory. */
export const FRONTEND_DIR = path.resolve(process.cwd(), "..", "frontend");
